# Workflow rules

- Edit files in the main repo directory (`/Users/haliterdogan/Documents/pingula/pingulacore-service/`). Do NOT create or duplicate files inside `.claude/worktrees/...`. The user works on the main checkout.
- Do NOT run `git commit` or `git push`. Apply changes to the working tree only. The user handles branching, committing, and pushing manually.
